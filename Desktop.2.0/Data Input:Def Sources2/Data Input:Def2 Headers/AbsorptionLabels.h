#ifndef _ABSORPLABEL#define _ABSORPLABEL #include	"GraphFunction.h"typedef struct  AbsorpLine{	float		lineEV;	float		intensity;	}AbsorpLine;class	AbsorptionLabel{	public:		short		element;		short		lineID;		float		lineEV;		float		intensity;		Rect		lineRect;				AbsorptionLabel	*next;		Handle 			GetInfoPtr(long *handleSize);		void				DoInit(void);		void				DoClose(void);		AbsorptionLabel* FindLines(double eV);		void 				DrawLines(GraphFunction *graph);		AbsorptionLabel*  				DoPopUp(Point thePoint,double eV);		void				DrawLabel(void);		void 				FindElementLines(short elNum);		void				FillMenu(PopUpMenu *theMenu);		AbsorptionLabel* 				FindLineFromMenu(short itemNo,short objectNo);		void 				DoCopy(AbsorptionLabel *copyInto);		Ptr 				GetTextualInfo(long *handleSize);};#endif