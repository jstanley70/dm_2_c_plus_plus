/* csroot.f -- translated by f2c (version 19941113).   You must link the resulting object file with the libraries:	-lf2c -lm   (in that order)*/#include "f2c.h"/* Subroutine */ int csroot_(rreal *xr, rreal *xi, rreal *yr, rreal *yi){    /* Builtin functions */   // double sqrt(doublerreal);    /* Local variables */    rreal s, ti, tr;    extern rreal pythag_(rreal *, rreal *);/*     BRANCH CHOSEN SO THAT YR .GE. 0.0 AND SIGN(YI) .EQ. SIGN(XI) *//* ***FIRST EXECUTABLE STATEMENT  CSROOT */    tr = *xr;    ti = *xi;    s = sqrt((pythag_(&tr, &ti) + abs(tr)) * .5f);    if (tr >= 0.f) {	*yr = s;    }    if (ti < 0.f) {	s = -s;    }    if (tr <= 0.f) {	*yi = s;    }    if (tr < 0.f) {	*yr = ti / *yi * .5f;    }    if (tr > 0.f) {	*yi = ti / *yr * .5f;    }    return 0;} /* csroot_ */